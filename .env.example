# Copy to `.env` and fill in (never commit `.env`):
FREEIMAGE_API_KEY=your_freeimage_host_api_key_here
PORT=28407
# Optional: SQLite state file (conversations, responses, files survive restart)
#GEMINI_DB_PATH=/path/to/gem2oai.db
